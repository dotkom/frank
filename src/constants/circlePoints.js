const width = 500;
const r = 700 / (Math.PI * 2);
const d = r * 2;
const y = (width - d) / 2;
const pathData = [
  "M",
  width / 2,
  y,
  "a",
  r,
  r,
  0,
  0,
  1,
  0,
  d,
  "a",
  r,
  r,
  0,
  0,
  1,
  0,
  -d,
];

const number_people = [
    [700 / 12, 700],
    [(700 / 12) * 2, 700],
    [(700 / 12) * 3, 700],
    [(700 / 12) * 4, 700],
    [(700 / 12) * 5, 700],
    [(700 / 12) * 6, 700],
    [(700 / 12) * 7, 700],
    [(700 / 12) * 8, 700],
    [(700 / 12) * 9, 700],
    [(700 / 12) * 10, 700],
    [(700 / 12) * 11, 700],
    [(700 / 12) * 12, 700],
  ];

export {width, pathData, number_people}